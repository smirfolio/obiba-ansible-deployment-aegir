---
branch: 7x21x
aegir_home: "{{ ansible_env.HOME }}"
date: "{{ ansible_date_time.epoch }}"
aegir_user: aegir
alias_domain: maelstrom-site-aegir.local
platform: staging_{{ branch }}_{{ date }}_mr_portal
aegir_makefile: "{{ platform }}.make"
tmp_path: tmp/
drush_cache: .drush/cache/
composer_cache: .composer/cache/
ENV_DEV: false
mica_version: 21.1
agate_version: 2.3
theme_bootstrap_version: 4.2
js_lib_version: 2.1.3